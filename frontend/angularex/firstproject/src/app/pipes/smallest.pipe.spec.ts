import { SmallestPipe } from './smallest.pipe';

describe('SmallestPipe', () => {
  it('create an instance', () => {
    const pipe = new SmallestPipe();
    expect(pipe).toBeTruthy();
  });
});
