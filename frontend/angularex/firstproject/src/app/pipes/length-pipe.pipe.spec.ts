import { LengthPipe } from './length-pipe.pipe';

describe('LengthPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LengthPipe();
    expect(pipe).toBeTruthy();
  });
});
